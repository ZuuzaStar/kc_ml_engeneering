# ML Сервис Рекомендации Фильмов

Современный ML сервис для рекомендации фильмов на основе текстовых запросов пользователей. Система использует семантический поиск и векторные эмбеддинги для точного подбора фильмов по описанию предпочтений.

## 🎯 Основные возможности

- **Семантический поиск фильмов** - поиск по описанию предпочтений пользователя
- **Рекомендательная система** - умные рекомендации на основе векторных эмбеддингов
- **REST API** - удобный API для интеграции с внешними системами
- **Web UI** - современный веб-интерфейс для пользователей
- **Telegram Bot** - интерактивный бот для получения рекомендаций
- **База данных фильмов** - расширяемая коллекция с автоматической обработкой JSON файлов
- **Система пользователей** - регистрация, аутентификация и персональные рекомендации
- **Система балансов** - управление кредитами для получения рекомендаций

## 🏗️ Архитектура

```
├── app/                    # Основное приложение
│   ├── api.py             # FastAPI приложение с security middleware
│   ├── main.py            # Точка входа
│   ├── bot.py             # Telegram бот
│   ├── auth/              # Система аутентификации и безопасности
│   ├── database/          # Конфигурация БД
│   ├── models/            # SQLModel модели и Pydantic схемы
│   ├── routes/            # API маршруты (защищенные и публичные)
│   ├── services/          # Бизнес-логика и CRUD операции
│   ├── data/              # JSON файлы с фильмами
│   └── requirements.txt   # Зависимости Python
├── ml_worker/             # ML обработчик (отдельный контейнер)
├── nginx/                 # Веб-сервер (прокси)
└── docker-compose.yaml    # Оркестрация контейнеров
```

## 🚀 Быстрый старт

### Требования
- Docker и Docker Compose
- Python 3.11+

### Запуск
```bash
# Клонирование репозитория
git clone <repository-url>
cd kc_ml_engeneering

# Настройка переменных окружения
cp app/.env.example app/.env
cp ml_worker/.env.example ml_worker/.env

# Запуск всех сервисов
docker-compose up -d

# Проверка статуса
docker-compose ps
```

### Доступные сервисы
- **Web UI**: http://localhost/web
- **API**: http://localhost:8000
- **Документация API**: http://localhost:8000/docs
- **ML Worker**: Внутренний сервис для ML обработки

## 🔒 Система безопасности

### Аутентификация и авторизация
- **HTTP Basic Auth** - для API endpoints
- **Валидация паролей** - минимум 8 символов, верхний/нижний регистр, цифры
- **Rate Limiting** - 5 попыток входа за 5 минут
- **Защищенные endpoint'ы** - все критичные операции требуют авторизации

### Security Middleware
- **Security Headers** - X-Content-Type-Options, X-Frame-Options, X-XSS-Protection
- **Trusted Host** - защита от подозрительных заголовков
- **CORS** - ограниченные origins для безопасности
- **Логирование безопасности** - отслеживание всех попыток доступа

### Защищенные endpoint'ы
- `/api/users/balance` - получение баланса (требует авторизации)
- `/api/users/balance/adjust` - изменение баланса (требует авторизации)
- `/api/events/prediction/history` - история предсказаний (требует авторизации)
- `/api/events/prediction/new` - новые предсказания (требует авторизации)

## 📊 ML Модели

### Sentence Transformers
Система использует предобученные модели для генерации векторных эмбеддингов:
- **Модель**: `sentence-transformers/all-MiniLM-L6-v2`
- **Размерность эмбеддингов**: 384
- **Язык**: Многоязычная поддержка (русский, английский)

### Процесс рекомендаций
1. **Векторизация запроса** - преобразование текстового запроса в вектор
2. **Семантический поиск** - поиск похожих фильмов по косинусному сходству
3. **Ранжирование** - сортировка результатов по релевантности
4. **Фильтрация** - применение дополнительных фильтров (жанр, год)

## 🗄️ База данных

### PostgreSQL с расширениями
- **pgvector** - для векторных операций и семантического поиска
- **SQLModel** - современный ORM для работы с данными
- **Автоматическая миграция** - схема создается автоматически при запуске

### Модели данных
- **Movie** - информация о фильмах с векторными эмбеддингами
- **User** - пользователи системы с флагом администратора
- **Prediction** - история рекомендаций пользователей
- **Wallet** - система балансов и кредитов
- **Transaction** - транзакции (пополнение, списание, предсказания)

## 🌐 Web UI

### Основные функции
- **Регистрация и авторизация** - с валидацией данных
- **Управление балансом** - пополнение и просмотр
- **Запрос рекомендаций** - текстовый ввод для поиска фильмов
- **История предсказаний** - отдельная страница с результатами
- **История транзакций** - отдельная страница с операциями

### Особенности интерфейса
- **Адаптивный дизайн** - работает на всех устройствах
- **Динамические блоки** - изменение UI в зависимости от статуса авторизации
- **Безопасность** - все операции требуют аутентификации

## 🔧 API Endpoints

### Публичные маршруты
- `GET /` - информация о сервисе
- `GET /health` - проверка состояния сервиса
- `POST /api/users/signup` - регистрация пользователя
- `POST /api/users/signin` - аутентификация пользователя

### Защищенные маршруты (требуют авторизации)
- `GET /api/users/balance` - получение баланса
- `POST /api/users/balance/adjust` - изменение баланса
- `GET /api/users/transaction/history` - история транзакций
- `GET /api/events/prediction/history` - история предсказаний
- `POST /api/events/prediction/new` - новые предсказания

### Web UI маршруты
- `GET /web` - главная страница
- `GET /web/prediction-history` - история предсказаний
- `GET /web/transaction-history` - история транзакций

## 🤖 Telegram Bot

Интерактивный бот для получения рекомендаций:
- Поиск фильмов по описанию
- Персональные рекомендации
- История запросов
- Управление профилем

## 📁 Управление данными

### Добавление новых фильмов
1. Создайте JSON файл в `app/data/`
2. Добавьте фильмы в формате:
```json
[
  {
    "title": "Название фильма",
    "description": "Описание фильма",
    "year": 2023,
    "genres": ["жанр1", "жанр2"]
  }
]
```
3. Система автоматически обработает все JSON файлы при запуске

### Система балансов
- **Начальный бонус** - 20 кредитов при регистрации
- **Стоимость предсказания** - 10 кредитов для обычных пользователей, 0 для администраторов
- **Пополнение баланса** - через защищенный endpoint

## 📈 Мониторинг и логирование

### Логирование
- **Loguru** - структурированное логирование
- **Security logging** - отслеживание попыток доступа
- **Performance monitoring** - время обработки запросов
- **Error tracking** - детальное логирование ошибок

### Метрики
- Количество пользователей
- Статистика предсказаний
- Производительность ML модели
- Использование ресурсов

## 🐳 Docker развертывание

### Сервисы
- **app** - основное FastAPI приложение
- **ml_worker** - ML обработчик
- **database** - PostgreSQL с pgvector
- **rabbitmq** - очередь сообщений для RPC
- **web-proxy** - Nginx прокси

### Переменные окружения
- `app/.env` - настройки основного приложения
- `ml_worker/.env` - настройки ML воркера
- `docker-compose.yaml` - конфигурация сервисов

## 🔧 Разработка

### Локальная разработка
```bash
# Установка зависимостей
pip install -r app/requirements.txt

# Запуск в режиме разработки
cd app
uvicorn api:app --reload --host 0.0.0.0 --port 8000
```

### Тестирование
- API endpoints тестируются через Swagger UI
- Web UI тестируется в браузере
- ML функциональность тестируется через RPC вызовы

## 📚 Дополнительная документация

- [Рекомендации по безопасности](SECURITY_RECOMMENDATIONS.md)
- [Описание данных фильмов](app/data/README.md)

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте feature branch
3. Внесите изменения
4. Создайте Pull Request



